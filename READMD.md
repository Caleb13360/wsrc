
# WsrcApp
Angular web app for users
## Development
```bash
cd wsrc-app; docker system prune -f; docker-compose up --build
```
Once the server is running, open your browser and navigate to [wsrc](http://localhost:4200/)